// Single source of truth for the displayed app version.
// (package.json can't be imported by Vite without extra config, so the
// release version is mirrored here and referenced by Sidebar/LandingPage.)
export const APP_VERSION = '3.0.4';
